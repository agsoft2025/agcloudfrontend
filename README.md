# AG Cloud Frontend

SvelteKit project using TypeScript with strict type checking enabled.

## Scripts

- `npm run dev` starts the development server.
- `npm run check` runs Svelte and TypeScript checks.
- `npm run build` creates a production build.
